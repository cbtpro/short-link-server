http:
  host: "localhost"
  port: 8080
secret:
  secret-key: secretKey
db:
  mysql:
    url: "localhost"
    port: 3306
    database: "short_links"
    username: "short_links"
    password: "short_links"

  redis:
    database: "localhost"
    port:
    db: 0
